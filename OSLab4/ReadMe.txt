# Readme

To run on the linserv1.cims.nyu.edu machine of NYU, firstly switch the compiler version:

module load gcc-9.2



To compile the code:

make

The executable is "iosched" and is under the current folder.



To clean it up:

make clean